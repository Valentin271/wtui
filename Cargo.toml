[package]
name = "wtui"
version = "0.3.1"
authors = ["Valentin271"]
license = "MIT"
edition = "2024"

[dependencies]
chrono = { version = "0.4.42", default-features = false, features = ["clock"] }
clipboard = "0.5.0"
ratatui = "0.30.0"
resolv-conf = "0.7.6"

[profile.release]
strip = true
